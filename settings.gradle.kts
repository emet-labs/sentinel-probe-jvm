// settings.gradle.kts — the JVM Probe SDK is a single-module Gradle build.
//
// Generated proto types live under gen/ (gitignored, produced by
// tools/generate-jvm-sdk.sh) and are wired into the main source set in build.gradle.kts,
// so there is no protobuf Gradle plugin and no separate generated-code module.

rootProject.name = "sentinel-probe-sdk-jvm"
